{
  "name": "musickku-backend",
  "version": "1.0.0",
  "description": "MusicKu Backend API - YouTube search, stream & download",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-yt": "pip install yt-dlp --break-system-packages || pip3 install yt-dlp"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
